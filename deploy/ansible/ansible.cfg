[defaults]
inventory = inventory.ini
# the box is reached via Cloudflare Access, not raw IP
host_key_checking = False
retry_files_enabled = False
# YAML output via the built-in default callback
callback_result_format = yaml

vault_password_file = .vault_pass

[ssh_connection]
pipelining = True
