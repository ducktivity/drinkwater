# OS generated files
.DS_Store
Thumbs.db

# Web/Desktop (Node)
web-desktop/node_modules/
web-desktop/dist/
web-desktop/src-tauri/target/

# Backend (Go)
backend/bin/
backend/*.exe
backend/*.app
backend/tmp/

# Mobile (Flutter)
mobile/.dart_tool/
mobile/build/
mobile/.flutter-plugins
mobile/.flutter-plugins-dependencies

# Secrets — real secrets never live in git. Prod values stay in your local
# .env.prod (shipped to the box by deploy/remote-deploy.sh); dev values in .env.
.env
.env.prod

# Agents
AGENTS.md
CLAUDE.md
.agent/