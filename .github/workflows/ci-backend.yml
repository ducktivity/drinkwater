name: Backend CI

# Validation gate for the Go backend on every PR/main push. Runs: go fmt, go vet, go build, go test, staticcheck, gosec.

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  check:
    name: Verify backend
    uses: ./.github/workflows/_backend-check.yml
