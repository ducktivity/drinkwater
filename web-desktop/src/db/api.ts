import createClient from 'openapi-fetch'
import { type paths } from '../types/schema' // Auto-generated by openapi-typescript

// Initialize the fetch client pointing to our local Go Chi server
export const apiClient = createClient<paths>({
  baseUrl: 'http://localhost:8080',
})
